import { login, logout, getInfo, refreshToken } from '@/api/login'
import { getToken, setToken, setExpiresIn, removeToken } from '@/utils/auth'

const constip = {
  state: {
    ipp:'127.0.0.1'
  },

  mutations: {

  },

  actions: {

    },







}

export default constip
